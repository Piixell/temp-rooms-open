// Funzione semplificata per verificare le funzionalità
// In questa versione senza subscription, tutte le funzionalità sono disponibili
function checkPremiumFeature(guildId, feature) {
  // Tutte le funzionalità sono disponibili in questa versione semplificata
  return {
    allowed: true
  };
}

module.exports = {
  checkPremiumFeature
};
